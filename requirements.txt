# Dependencias para Babelcomics4
# Comic management application with GTK4

# Database ORM
SQLAlchemy>=2.0.0

# HTTP requests for ComicVine API
requests>=2.31.0

# Image processing
Pillow>=10.0.0

# Comic file extraction (CBR, CB7)
# Requiere comandos del sistema: unrar, 7z
rarfile>=4.0  # Para archivos CBR/RAR
py7zr>=0.20.0  # Para archivos CB7/7Z

# Image embeddings for auto-classification (CLIP model)
# IMPORTANTE: torch debe instalarse por separado con el build de CUDA correcto:
#
#   GTX 1070 / GPUs Pascal (CUDA capability 6.1) - usar build cu126:
#     pip install torch==2.9.0+cu126 --index-url https://download.pytorch.org/whl/cu126
#
#   RTX 3000+ / GPUs modernas (CUDA capability >= 7.0) - usar build por defecto:
#     pip install torch>=2.9.0
#
#   Solo CPU (sin GPU):
#     pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# NOTA: Los builds cu128+ eliminaron soporte para Pascal (sm_61).
#       El build cu126 es el último que soporta GTX 1070/1080.
transformers>=4.35.0
numpy<2

# GTK4 and libadwaita bindings
# IMPORTANTE: Instalar dependencias del sistema PRIMERO:
# Arch: sudo pacman -S python-gobject gtk4 libadwaita gobject-introspection
# Ubuntu/Debian: sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-4.0 gir1.2-adw-1 libgirepository1.0-dev
# Fedora: sudo dnf install python3-gobject gtk4-devel libadwaita-devel gobject-introspection-devel
#
# IMPORTANTE: Crear el venv con --system-site-packages para acceder a gi/GTK4:
#   python -m venv --system-site-packages .venv
PyGObject>=3.42.0

# Optional: Development tools
# flake8>=6.0.0
# black>=23.0.0