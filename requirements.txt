# Dependencias para Babelcomics4
# Comic management application with GTK4

# Database ORM
SQLAlchemy>=2.0.0

# HTTP requests for ComicVine API
requests>=2.31.0

# Image processing
Pillow>=10.0.0

# Comic file extraction (CBR, CB7)
# Requiere comandos del sistema: unrar, 7z
rarfile>=4.0  # Para archivos CBR/RAR
py7zr>=0.20.0  # Para archivos CB7/7Z

# Image embeddings for auto-classification (GTX 1070 compatible versions)
# Para GPU GTX 1070 (CUDA 6.1), usar Python 3.11 y ejecutar:
#   pip install torch==1.13.1+cu117 --extra-index-url https://download.pytorch.org/whl/cu117
#   pip install transformers==4.25.1 numpy<2
# Para CPU o GPU moderna (RTX 3000+), usar las versiones nuevas comentadas abajo

# Versiones compatibles con GTX 1070 (Python 3.11)
# Instalar manualmente con: pip install torch==1.13.1+cu117 --extra-index-url https://download.pytorch.org/whl/cu117
transformers==4.25.1
numpy<2

# Versiones para GPU moderna (descomentar si tienes RTX 3000+ o usando CPU en Python 3.13)
# torch>=2.0.0
# transformers>=4.35.0
# numpy>=1.24.0

# GTK4 and libadwaita bindings
# IMPORTANTE: Instalar dependencias del sistema PRIMERO:
# Arch: sudo pacman -S python-gobject gtk4 libadwaita gobject-introspection
# Ubuntu/Debian: sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-4.0 gir1.2-adw-1 libgirepository1.0-dev
# Fedora: sudo dnf install python3-gobject gtk4-devel libadwaita-devel gobject-introspection-devel

# Luego instalar en venv:
PyGObject>=3.42.0

# Optional: Development tools
# flake8>=6.0.0
# black>=23.0.0